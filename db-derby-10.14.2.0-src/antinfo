Buildfile: /Users/wangkai/Downloads/db-derby-10.14.2.0-src/build.xml

checkCompilerLevel:

checkVMLevel:

junit_check:

install_junit:
      [get] Getting: http://repo1.maven.org/maven2/junit/junit/3.8.2/junit-3.8.2.jar
      [get] To: /Users/wangkai/Downloads/db-derby-10.14.2.0-src/tools/java/junit.jar
      [get] Error opening connection java.io.IOException: Server returned HTTP response code: 501 for URL: http://repo1.maven.org/maven2/junit/junit/3.8.2/junit-3.8.2.jar
      [get] Error opening connection java.io.IOException: Server returned HTTP response code: 501 for URL: http://repo1.maven.org/maven2/junit/junit/3.8.2/junit-3.8.2.jar
      [get] Error opening connection java.io.IOException: Server returned HTTP response code: 501 for URL: http://repo1.maven.org/maven2/junit/junit/3.8.2/junit-3.8.2.jar
      [get] Can't get http://repo1.maven.org/maven2/junit/junit/3.8.2/junit-3.8.2.jar to /Users/wangkai/Downloads/db-derby-10.14.2.0-src/tools/java/junit.jar

prebuild:

compile:

build:

setCompilerProperties:

init:

make-generated-dirs:

make-release-dirs:

ensuresanitystate:

setissane:

felixStubs:

showenv:
     [echo] 
     [echo] Ant environment:
     [echo]   Base Directory: /Users/wangkai/Downloads/db-derby-10.14.2.0-src
     [echo]   Build output: /Users/wangkai/Downloads/db-derby-10.14.2.0-src/classes
     [echo]   Compiler: modern
     [echo]   Sane = true
     [echo]   Proceed = no
     [echo] 

getstate:

state:

shared:
     [echo] building generated sanity info

compile:

shared:

engine:

engine:

compile_reference:

compile_authentication:

authentication:

compile_security:

security:

compile_io:

storage:

iapi:

compile_types:

compile_util:

compile_iapi_db:
    [javac] Compiling 1 source file to /Users/wangkai/Downloads/db-derby-10.14.2.0-src/classes

checkVMLevel:

compile_jdbc4:

compile_jdbc42:

compile:

compile:

compile:

compile_crypto:

compile:

compile:

compile_mbeans:

compile_database:

database:

compile_vti:

vti:

compile_agg:

agg:

compile_impl:

compile_impl_services:

compile_impl_io:

parser:

chkparser:

genParser:

compile:
    [javac] Compiling 1 source file to /Users/wangkai/Downloads/db-derby-10.14.2.0-src/classes

compile_impl_store:

compile:

compile:

checkVMLevel:

compile_jdbc4:

compile_jdbc42:

compile:

checkVMLevel:

compile_jdbc4:

compile_jdbc42:

jdbc:

compile_osgi:

osgi:

compile_catalog:

catalog:

compile_diag:

diag:

build:

build_base:

compile:

build:

build:

init:

buildMessages:

verifysplit:

split:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

splitprops:

verifysplit:

noSplit:

chksplit:

doSplit:

locales:

cscuptodate:

class_size_catalog:

storeless:

storeless:

tools:

tools_base:

compile_tools_iapi:

parser:

chkparser:

genParser:

chkparser:

genParser:

compile:

compile:

tools:

copyfiles:

locales:

tools:

drda:

drda:

compile:

drda:

compile:

checkservletjar:

servlet:

copyfiles:

drda:

copyfiles:

locales:

odbcprops:

verifyodbcgen:

noODBC:

chkodbcgen:

odbcMeta:

tools_run:

compile_tools_iapi_run:

client:

checkVMLevel:

compile:

compile_jdbc42:

client:

optional:

optional:

build:

build_base:

compile:

build:

build:

ckversioninfo:

writeversioninfo:

versioninfo:

checklocaleinfo:

checkmessages-done:

checkmessages:

localeinfo:

check-binscripts:

binscripts:

buildsource:

BUILD SUCCESSFUL
Total time: 2 seconds
